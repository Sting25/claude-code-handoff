{
  "name": "claude-code-handoff",
  "description": "Session-state handoff for Claude Code: hooks auto-snapshot at SessionEnd/PreCompact and auto-load at SessionStart. Rebuilds lost handoffs from per-turn backups, keeps rotating history, nudges at ~40% context, and HMAC-signs handoffs so cloned repos can't inject trusted rules.",
  "owner": {
    "name": "Christopher Chadwick",
    "url": "https://github.com/Sting25"
  },
  "plugins": [
    {
      "name": "claude-code-handoff",
      "source": "./",
      "description": "Session-state handoff for Claude Code: hooks auto-snapshot at SessionEnd/PreCompact and auto-load at SessionStart. Rebuilds lost handoffs from per-turn backups, keeps rotating history, nudges at ~40% context, and HMAC-signs handoffs so cloned repos can't inject trusted rules.",
      "author": {
        "name": "Christopher Chadwick",
        "url": "https://github.com/Sting25"
      },
      "homepage": "https://github.com/Sting25/claude-code-handoff"
    }
  ]
}
